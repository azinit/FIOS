from math import *
import font

b = 34.58 * 10**2
a = 9876.5
x = -tan(sqrt(3))
if b*b > a*x:
    y = b * sqrt(abs(2*cos(6*x - 0.3))) + e**(sin(x))
    print(y)
    y = sqrt(tan(abs(3*x + 0.6))) + e**(-x)

print(y)